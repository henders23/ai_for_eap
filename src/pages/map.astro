---
/**
 * Framework map — the TEAP × UNESCO coverage matrix, fed by kept-artefact
 * evidence via the FrameworkMap island.
 */
import Base from '../layouts/Base.astro';
import FrameworkMap from '../components/FrameworkMap.tsx';
import '../styles/framework-map.css';
---

<Base
  title="Framework map"
  description="How your kept work maps onto the UNESCO AI competency framework and the BALEAP TEAP criteria — a coverage matrix fed by each module's Map it tag."
  maxWidth="none"
>
  <FrameworkMap client:load />
</Base>
